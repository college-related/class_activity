var numbers = [1, 4, 5.5];
// var name = new Array();

// to add new elements in the array at the last
numbers.push(5,7);

// to delete last element
numbers.pop();

// to add elements at first of the array
numbers.unshift(8, 6);

// to delete the first item of the array
numbers.shift();

// to delete selected items from the aray
numbers.splice(1, 2);

console.log(numbers);

numbers.find(2);

/*
    Array can have different data types in js
    No need to declare the size of arrar in js
*/
